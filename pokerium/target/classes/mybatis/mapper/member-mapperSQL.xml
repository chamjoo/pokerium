<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="member">
  	
  	
	<select id="selectLoginMember" parameterType="member" resultType="member">
		
		SELECT
			MI_NO as miNo,
		    MI_ID as miId,
		    MI_PWD as miPwd,
		    MI_NAME as miName,
		    MI_BIRTH as miBirth,
		    MI_GENDER as miGender,
		    MI_EMAIL as miEmail,
		    MI_ISMAIL as miIsmail,
		    MI_PHONE as miPhone,
		    MI_ISSNS as miIssns,
		    MI_TEL as miTel,
		    MI_ISACTIVE as miIsactive,
		    MI_REBANK as miRebank,
		    MI_ACCOUNT as miAccount,
		    MI_REGTIME as miRegtime,
		    MI_DROPTIME as miDroptime,
		    MI_LASTLOGIN as miLastlogin
		FROM 
			T_MEMBER_INFO
		WHERE
			MI_ID = #{miId} AND MI_PWD = #{miPwd} AND MI_ISACTIVE = "Y"

	</select>
  	
  	<insert id="insertMember" parameterType="member">
  		INSERT INTO 
  			T_MEMBER_INFO	
  			(MI_ID,
  			 MI_PWD, 
  			 MI_EMAIL,
  			 MI_NAME,
  			 MI_GENDER,
  			 MI_BIRTH,
  			 MI_PHONE 
  			) 
  		VALUES	
  			(#{miId}, #{miPwd}, #{miEmail}, #{miName}, #{miGender}, #{miBirth}, #{miPhone})
  	
  	</insert>
  	
 
</mapper>
